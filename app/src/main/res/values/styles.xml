<resources>

    <!-- BASE THEME -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MaterialBase" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- APP BAR -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


    <!-- LABEL VIEW TEME'S -->
    <style name="LabelViewDefault">
        <item name="backgroundType">@drawable/label_shape_unselected</item>
        <item name="itemsColor">@color/labelUnselectedItems</item>
    </style>

    <style name="LabelViewSelected">
        <item name="backgroundType">@drawable/label_shape_selected</item>
        <item name="itemsColor">@color/labelSelectedItems</item>
    </style>

</resources>
